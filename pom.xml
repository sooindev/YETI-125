<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.irion</groupId>
  <artifactId>Yeti-125</artifactId>
  <version>1.0.0</version>
  <packaging>war</packaging>

  <name>Yeti 125</name>
  <description>이리온 팬 웹사이트</description>

  <properties>
    <java.version>1.8</java.version>
    <spring.version>5.3.39</spring.version>
    <mybatis.version>3.5.13</mybatis.version>
    <mybatis-spring.version>2.1.1</mybatis-spring.version>
    <mariadb.version>3.1.4</mariadb.version>
    <jackson.version>2.15.2</jackson.version>
    <hibernate-validator.version>6.2.5.Final</hibernate-validator.version>
    <slf4j.version>1.7.36</slf4j.version>
    <logback.version>1.2.13</logback.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>

  <dependencies>
    <!-- Spring Framework -->
    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-webmvc</artifactId>
      <version>${spring.version}</version>
    </dependency>
    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-jdbc</artifactId>
      <version>${spring.version}</version>
    </dependency>
    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-tx</artifactId>
      <version>${spring.version}</version>
    </dependency>

    <!-- MyBatis -->
    <dependency>
      <groupId>org.mybatis</groupId>
      <artifactId>mybatis</artifactId>
      <version>${mybatis.version}</version>
    </dependency>
    <dependency>
      <groupId>org.mybatis</groupId>
      <artifactId>mybatis-spring</artifactId>
      <version>${mybatis-spring.version}</version>
    </dependency>

    <!--
      MariaDB

      드라이버가 waffle-jna 를 딸려 온다. 윈도우 SSPI 통합 인증용이고,
      그게 다시 jna / jna-platform / caffeine 을 끌고 온다. 합쳐서 4.5MB 인데
      리눅스 톰캣 배포에서는 한 줄도 실행되지 않는다.

      드라이버가 실제로 쓰는 것은 사용자명/비밀번호 인증뿐이다.
    -->
    <dependency>
      <groupId>org.mariadb.jdbc</groupId>
      <artifactId>mariadb-java-client</artifactId>
      <version>${mariadb.version}</version>
      <exclusions>
        <exclusion>
          <groupId>com.github.waffle</groupId>
          <artifactId>waffle-jna</artifactId>
        </exclusion>
      </exclusions>
    </dependency>

    <!-- HikariCP Connection Pool -->
    <dependency>
      <groupId>com.zaxxer</groupId>
      <artifactId>HikariCP</artifactId>
      <version>4.0.3</version>
    </dependency>

    <!-- Servlet & JSP -->
    <dependency>
      <groupId>javax.servlet</groupId>
      <artifactId>javax.servlet-api</artifactId>
      <version>4.0.1</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>javax.servlet.jsp</groupId>
      <artifactId>javax.servlet.jsp-api</artifactId>
      <version>2.3.3</version>
      <scope>provided</scope>
    </dependency>
    <!--
      jstl 은 뺐다. WEB-INF/views 의 JSP 두 개(404, 500)가 taglib 를
      하나도 쓰지 않는다. 다시 쓰게 되면 그때 되살릴 것.
    -->

    <!-- Jackson JSON -->
    <dependency>
      <groupId>com.fasterxml.jackson.core</groupId>
      <artifactId>jackson-databind</artifactId>
      <version>${jackson.version}</version>
    </dependency>

    <!--
      Bean Validation (javax.validation)

      6.2.x 가 javax 네임스페이스를 쓰는 마지막 계열이다. 7.x 부터는
      jakarta 로 넘어가 톰캣 9 / Spring 5.3 조합에서 쓸 수 없다.

      메시지 보간에 EL 구현체가 필요하지만, servlet-context.xml 에서
      ParameterMessageInterpolator 를 지정해 EL 없이 돌린다. 컨테이너의
      EL 버전에 끌려다니지 않으려는 것이다.
    -->
    <dependency>
      <groupId>org.hibernate.validator</groupId>
      <artifactId>hibernate-validator</artifactId>
      <version>${hibernate-validator.version}</version>
    </dependency>

    <!-- Logging -->
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-api</artifactId>
      <version>${slf4j.version}</version>
    </dependency>
    <dependency>
      <groupId>ch.qos.logback</groupId>
      <artifactId>logback-classic</artifactId>
      <version>${logback.version}</version>
    </dependency>

    <!-- Test -->
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>4.13.2</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <finalName>yeti-125</finalName>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.11.0</version>
        <configuration>
          <source>${java.version}</source>
          <target>${java.version}</target>
          <encoding>UTF-8</encoding>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-war-plugin</artifactId>
        <version>3.4.0</version>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <!--
      배포용 빌드 프로파일.

        mvn clean package          → 로컬 개발용 (src/main/resources 그대로)
        mvn clean package -Pprod   → 운영 배포용

      src/main/resources/properties/database.properties 는 항상 로컬 설정을
      유지하고, 운영 설정은 src/main/resources-prod 에 따로 둔다. prod 로
      빌드할 때만 그 디렉터리를 target/classes 위에 덮어써서 war 에 운영
      설정이 들어가게 한다. 배포할 때 파일을 손으로 바꿔치기할 필요가 없다.

      두 파일 모두 .gitignore 대상이라 저장소에는 올라가지 않는다.
    -->
    <profile>
      <id>prod</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-resources-plugin</artifactId>
            <version>3.3.1</version>
            <executions>
              <execution>
                <id>override-config-for-prod</id>
                <!-- 기본 리소스 복사가 끝난 뒤 덮어써야 하므로 prepare-package -->
                <phase>prepare-package</phase>
                <goals>
                  <goal>copy-resources</goal>
                </goals>
                <configuration>
                  <outputDirectory>${project.build.outputDirectory}</outputDirectory>
                  <overwrite>true</overwrite>
                  <resources>
                    <resource>
                      <directory>${project.basedir}/src/main/resources-prod</directory>
                    </resource>
                  </resources>
                </configuration>
              </execution>
            </executions>
          </plugin>

          <!--
            안전장치: 자리표시자가 남은 채로 배포용 war 가 만들어지면
            운영에서 DB 접속이 실패한다. 그런 war 는 아예 만들어지지
            않도록 빌드를 중단시킨다.
          -->
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-antrun-plugin</artifactId>
            <version>3.1.0</version>
            <executions>
              <execution>
                <id>verify-prod-config</id>
                <phase>prepare-package</phase>
                <goals>
                  <goal>run</goal>
                </goals>
                <configuration>
                  <target>
                    <loadfile property="db.config"
                              srcFile="${project.build.outputDirectory}/properties/database.properties"
                              encoding="UTF-8"/>
                    <fail message="운영 DB 설정이 채워지지 않았습니다. src/main/resources-prod/properties/database.properties 의 DB_USERNAME / DB_PASSWORD (CHANGE_ME_) 를 실제 값으로 바꾸세요.">
                      <condition>
                        <contains string="${db.config}" substring="CHANGE_ME"/>
                      </condition>
                    </fail>
                    <fail message="운영 빌드인데 db.url 이 운영 주소가 아닙니다. src/main/resources-prod 설정을 확인하세요.">
                      <condition>
                        <not>
                          <contains string="${db.config}" substring="localhost:3306"/>
                        </not>
                      </condition>
                    </fail>
                  </target>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>