<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.irion</groupId>
  <artifactId>Yeti-125</artifactId>
  <version>1.0.0</version>
  <packaging>war</packaging>

  <name>Yeti 125</name>
  <description>이리온 팬 웹사이트</description>

  <properties>
    <java.version>1.8</java.version>
    <spring.version>5.3.39</spring.version>
    <mybatis.version>3.5.19</mybatis.version>
    <mybatis-spring.version>2.1.1</mybatis-spring.version>
    <mariadb.version>3.5.10</mariadb.version>
    <jackson.version>2.22.2</jackson.version>
    <!--
      여기부터 셋(validator·slf4j·logback)은 일부러 안 올린다 — 다음 계열이
      jakarta 네임스페이스나 자바 11 이상을 요구해 지금 조합에서 못 쓴다.
      아래 dependency 에 직접 적힌 HikariCP 4.0.3 도 같은 이유다 (5.1.0 은 자바 11 이상).
    -->
    <hibernate-validator.version>6.2.5.Final</hibernate-validator.version>
    <slf4j.version>1.7.36</slf4j.version>
    <logback.version>1.2.13</logback.version>

    <!-- -Dnvd.api.key=... 또는 환경변수로 덮어쓴다. 저장소에는 비워 둔다 -->
    <nvd.api.key>${env.NVD_API_KEY}</nvd.api.key>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>

  <dependencies>
    <!-- Spring Framework -->
    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-webmvc</artifactId>
      <version>${spring.version}</version>
    </dependency>
    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-jdbc</artifactId>
      <version>${spring.version}</version>
    </dependency>
    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-tx</artifactId>
      <version>${spring.version}</version>
    </dependency>

    <!-- MyBatis -->
    <dependency>
      <groupId>org.mybatis</groupId>
      <artifactId>mybatis</artifactId>
      <version>${mybatis.version}</version>
    </dependency>
    <dependency>
      <groupId>org.mybatis</groupId>
      <artifactId>mybatis-spring</artifactId>
      <version>${mybatis-spring.version}</version>
    </dependency>

    <!-- waffle-jna(윈도우 SSPI)와 딸린 것들 4.5MB 를 뺀다 — 리눅스 배포에서는 안 쓴다 -->
    <dependency>
      <groupId>org.mariadb.jdbc</groupId>
      <artifactId>mariadb-java-client</artifactId>
      <version>${mariadb.version}</version>
      <exclusions>
        <exclusion>
          <groupId>com.github.waffle</groupId>
          <artifactId>waffle-jna</artifactId>
        </exclusion>
      </exclusions>
    </dependency>

    <!-- HikariCP Connection Pool -->
    <dependency>
      <groupId>com.zaxxer</groupId>
      <artifactId>HikariCP</artifactId>
      <version>4.0.3</version>
    </dependency>

    <!-- Servlet & JSP -->
    <dependency>
      <groupId>javax.servlet</groupId>
      <artifactId>javax.servlet-api</artifactId>
      <version>4.0.1</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>javax.servlet.jsp</groupId>
      <artifactId>javax.servlet.jsp-api</artifactId>
      <version>2.3.3</version>
      <scope>provided</scope>
    </dependency>
    <!-- jstl 은 뺐다 — JSP 가 taglib 를 하나도 쓰지 않는다 -->

    <!-- Jackson JSON -->
    <dependency>
      <groupId>com.fasterxml.jackson.core</groupId>
      <artifactId>jackson-databind</artifactId>
      <version>${jackson.version}</version>
    </dependency>

    <!-- 6.2.x 가 javax 네임스페이스의 마지막 계열이다. 7.x 는 jakarta 라 지금 조합에서 못 쓴다 -->
    <dependency>
      <groupId>org.hibernate.validator</groupId>
      <artifactId>hibernate-validator</artifactId>
      <version>${hibernate-validator.version}</version>
    </dependency>

    <!-- Logging -->
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-api</artifactId>
      <version>${slf4j.version}</version>
    </dependency>
    <dependency>
      <groupId>ch.qos.logback</groupId>
      <artifactId>logback-classic</artifactId>
      <version>${logback.version}</version>
    </dependency>

    <!-- Test -->
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>4.13.2</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <finalName>yeti-125</finalName>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.11.0</version>
        <configuration>
          <source>${java.version}</source>
          <target>${java.version}</target>
          <encoding>UTF-8</encoding>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-war-plugin</artifactId>
        <version>3.4.0</version>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <!--
      배포용 빌드 (mvn clean package -Pprod).
      운영 설정은 resources-prod 에 따로 두고 prod 빌드에서만 덮어쓴다. 둘 다 .gitignore 대상.
    -->
    <profile>
      <id>prod</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-resources-plugin</artifactId>
            <version>3.3.1</version>
            <executions>
              <execution>
                <id>override-config-for-prod</id>
                <!-- 기본 리소스 복사가 끝난 뒤 덮어써야 하므로 prepare-package -->
                <phase>prepare-package</phase>
                <goals>
                  <goal>copy-resources</goal>
                </goals>
                <configuration>
                  <outputDirectory>${project.build.outputDirectory}</outputDirectory>
                  <overwrite>true</overwrite>
                  <resources>
                    <resource>
                      <directory>${project.basedir}/src/main/resources-prod</directory>
                    </resource>
                  </resources>
                </configuration>
              </execution>
            </executions>
          </plugin>

          <!-- 자리표시자가 남은 war 는 운영에서 DB 접속이 실패한다 — 만들어지지 않게 막는다 -->
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-antrun-plugin</artifactId>
            <version>3.1.0</version>
            <executions>
              <execution>
                <id>verify-prod-config</id>
                <phase>prepare-package</phase>
                <goals>
                  <goal>run</goal>
                </goals>
                <configuration>
                  <target>
                    <loadfile property="db.config"
                              srcFile="${project.build.outputDirectory}/properties/database.properties"
                              encoding="UTF-8"/>
                    <fail message="운영 DB 설정이 채워지지 않았습니다. src/main/resources-prod/properties/database.properties 의 DB_USERNAME / DB_PASSWORD (CHANGE_ME_) 를 실제 값으로 바꾸세요.">
                      <condition>
                        <contains string="${db.config}" substring="CHANGE_ME"/>
                      </condition>
                    </fail>
                    <fail message="운영 빌드인데 db.url 이 운영 주소가 아닙니다. src/main/resources-prod 설정을 확인하세요.">
                      <condition>
                        <not>
                          <contains string="${db.config}" substring="localhost:3306"/>
                        </not>
                      </condition>
                    </fail>
                  </target>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
    <!--
      의존성 취약점 스캔 — mvn -Psecurity verify -Dnvd.api.key=...
      NVD 키가 없으면 아예 돌지 않는다 (nvd.nist.gov/developers/request-an-api-key).
      기본 빌드에는 넣지 않는다. 키 없이 도는 감시는 .github/dependabot.yml 이 맡는다.
    -->
    <profile>
      <id>security</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.owasp</groupId>
            <artifactId>dependency-check-maven</artifactId>
            <version>13.0.0</version>
            <configuration>
              <nvdApiKey>${nvd.api.key}</nvdApiKey>
              <failBuildOnCVSS>7</failBuildOnCVSS>
              <formats>
                <format>HTML</format>
                <format>JSON</format>
              </formats>
              <suppressionFiles>
                <suppressionFile>${project.basedir}/dependency-check-suppress.xml</suppressionFile>
              </suppressionFiles>
              <!-- 테스트에서만 쓰는 라이브러리는 배포물에 없다 -->
              <skipTestScope>true</skipTestScope>
            </configuration>
            <executions>
              <execution>
                <goals>
                  <goal>check</goal>
                </goals>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>

  </profiles>
</project>