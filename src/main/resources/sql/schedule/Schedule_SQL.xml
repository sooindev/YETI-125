<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.irion.schedule.mapper.ScheduleMapper">

    <!--
      기간이 겹치는 일정만 고른다.

      종료일은 NULL 을 허용한다(단발 일정). SQL 에서 NULL 비교는 참이 아니라 NULL 이라
      그냥 두면 단발 일정이 한 건도 안 나온다. 예전에는 `OR end_date IS NULL` 로 살렸는데,
      그러면 "종료일이 없으면 무조건 통과"가 되어 시작일을 아예 안 본다 — 하한이 사라져
      아무리 오래된 일정도 전부 딸려 나왔다. 실제로 이틀치를 물어도 전체가 돌아왔다.

      COALESCE 로 "종료일이 없으면 시작일을 끝으로 본다" — 단발 일정은 그 한 점만 차지한다.
    -->

    <!-- 일정 목록 조회 (기간별, 관리자용 - 모든 일정) -->
    <select id="selectScheduleList" resultType="ScheduleVO">
        SELECT schedule_id
               ,title
               ,description
               ,schedule_type
               ,start_date
               ,end_date
               ,all_day_yn
               ,display_yn
               ,color
               ,reg_date
               ,mod_date
               ,del_yn
        FROM tb_schedule
        WHERE del_yn = 'N'
          AND start_date &lt;= #{endDate}
          AND COALESCE(end_date, start_date) &gt;= #{startDate}
        ORDER BY start_date ASC
    </select>

    <!-- 공개된 일정 목록 조회 (사용자용) -->
    <select id="selectDisplayScheduleList" resultType="ScheduleVO">
        SELECT schedule_id
               ,title
               ,description
               ,schedule_type
               ,start_date
               ,end_date
               ,all_day_yn
               ,display_yn
               ,color
               ,reg_date
               ,mod_date
               ,del_yn
        FROM tb_schedule
        WHERE del_yn = 'N'
          AND display_yn = 'Y'
          AND start_date &lt;= #{endDate}
          AND COALESCE(end_date, start_date) &gt;= #{startDate}
        ORDER BY start_date ASC
    </select>

    <!-- 일정 등록 -->
    <insert id="insertSchedule" parameterType="ScheduleVO" useGeneratedKeys="true" keyProperty="scheduleId">
        INSERT INTO tb_schedule (
            title
            ,description
            ,schedule_type
            ,start_date
            ,end_date
            ,all_day_yn
            ,display_yn
            ,color
        ) VALUES (
            #{title}
            ,#{description}
            ,#{scheduleType}
            ,#{startDate}
            ,#{endDate}
            ,#{allDayYn}
            ,#{displayYn}
            ,#{color}
        )
    </insert>

    <!-- 일정 수정 -->
    <!-- display_yn 은 값이 실려 왔을 때만 덮어쓴다 — 기본값을 정하면 숨긴 일정이 공개되거나 그 반대가 된다 -->
    <update id="updateSchedule" parameterType="ScheduleVO">
        UPDATE tb_schedule
        <set>
            title = #{title},
            description = #{description},
            schedule_type = #{scheduleType},
            start_date = #{startDate},
            end_date = #{endDate},
            all_day_yn = #{allDayYn},
            color = #{color},
            <if test="displayYn != null and displayYn != ''">
                display_yn = #{displayYn},
            </if>
        </set>
        WHERE schedule_id = #{scheduleId}
          AND del_yn = 'N'
    </update>

    <!-- 일정 삭제 (소프트 삭제) -->
    <update id="deleteSchedule" parameterType="Long">
        UPDATE tb_schedule
        SET del_yn = 'Y'
        WHERE schedule_id = #{scheduleId}
    </update>

</mapper>